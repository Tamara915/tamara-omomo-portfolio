{
  "name": "tamaraomomo-portfolio",
  "private": true,
  "description": "Static site — this package.json exists only so Netlify installs @netlify/blobs before bundling the serverless functions.",
  "dependencies": {
    "@netlify/blobs": "*"
  }
}
